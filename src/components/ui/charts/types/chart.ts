export interface ChartDimensions {
  minValue: number;
  maxValue: number;
  chartMinValue: number;
  chartMaxValue: number;
  totalRange: number;
  optimalLow: number;
  optimalHigh: number;
  normalLow: number;
  normalHigh: number;
}
