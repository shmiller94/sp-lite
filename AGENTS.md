# React App Agent Notes

## Commit Hygiene

- Before creating a commit or updating a PR from this repo, run `bun run fmt` and `bun run lint`.
- If either command fails, fix the issue or report the blocker before committing.
